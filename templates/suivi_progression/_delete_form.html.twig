<form method="post" action="{{ path('app_suivi_progression_delete', {'id': suivi_progression.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ suivi_progression.id) }}">
    <button class="btn">Delete</button>
</form>
